/* function corrFormatMoeda (e) {
    console.log("o charCode do backspace é :" + event.charCode);
    let twoLastValue = e.value.substring(e.value.length-2);
    console.log("a twoLastValue é " + twoLastValue);
    let woTwoLastValue = e.value.substring(3,e.value.length-2);
    console.log("a woTwoLastValue é: " + woTwoLastValue);
    e.value = "R$ " + woTwoLastValue + "," + twoLastValue
} */